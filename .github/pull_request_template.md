## Checklist

- [ ] **API org:** novas ou alteradas rotas em `app/api/org/**` usam `getMe`, `requireAdmin`, `requireAdminForOrgId` ou outro guard de `@/lib/apiOrgAuth` (não só Bearer / JWT “caseiro”).

## Descrição

<!-- O que mudou e por quê -->
